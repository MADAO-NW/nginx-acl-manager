# Nginx 多项目访问控制管理工具技术方案

## 1. 文档说明

- 文档状态：实施中
- 编写日期：2026-08-20
- 适用范围：第一版单机、多项目、多实例的 Nginx URL 白名单管理
- 需求依据：前序设计会话及 2026-08-14 已验证的 Debian 13 Sub2API Nginx 配置实践；该 Notion 实践作为默认 Profile 基线，不作为 Linux 发行版限制
- 命名说明：本文不确定产品展示名；目录和命令中的 `nginx-acl-manager` 仅沿用当前仓库标识

## 2. 背景与现状

当前 Sub2API 通过 Nginx 对敏感 URL 做来源 IP 限制：

1. Cloudflare 橙云作为公网入口，由独立的 real IP 配置恢复真实客户端 IP；
2. `geo` 根据 `$remote_addr` 判断客户端是否在白名单中；
3. `map` 联合判断 HTTP 方法、规范化 URI 和白名单状态；
4. 非白名单来源命中敏感 URL 时，在业务 `location` 中返回 `404`；
5. 当前规则由人工编辑 Nginx 文件，增加 IP、接口或实例时容易重复操作和误改。

现有受保护接口包括：

| 用途 | 方法与路径 |
| --- | --- |
| 保存系统设置 | `PUT /api/v1/admin/settings` |
| 导出账号凭证 | `GET/HEAD /api/v1/admin/accounts/data` |
| 禁用双因素认证 | `POST /api/v1/user/totp/disable` |
| 修改账号配置 | `PUT /api/v1/admin/accounts/{数字ID}` |

后续除 Sub2API 外，还可能接入 CLIProxyAPI 等项目；同一项目也可能在不同本地端口运行多个实例。管理能力因此不能绑定到某个业务项目，也不能把不同实例的规则写入同一份执行片段。

## 3. 建设目标

第一版实现以下能力：

1. 通过表单管理项目、服务实例、白名单和受保护 URL，用户不直接编辑 YAML、JSON 或 Nginx；
2. 按“项目 → 实例”隔离配置，同一项目的不同本地端口可使用不同白名单和规则；
3. 白名单支持 IPv4、IPv6、CIDR，并支持用 `0.0.0.0` 表示全部 IPv4；
4. 接口规则具有稳定 ID，可新增、编辑、启用、停用和删除；
5. 所有编辑先保存为草稿，只有“验证并发布”才影响 Nginx；
6. 发布前展示生成配置和相对当前版本的差异；
7. 发布时通过 `nginx -T` 完成语法和实际 include 接入校验，失败不改变生效配置；
8. 保留不可变发布版本，支持按项目恢复历史配置；
9. Web 服务不以 root 运行，提权能力限制在固定发布入口内；
10. 第一版只提供一个管理员账号，所有管理页面必须登录后访问；
11. 安装时强制初始化管理员用户名和密码，并提供本机 CLI 修改能力；
12. Web 服务默认监听 `127.0.0.1:7582`，安装时允许覆盖端口；
13. 不固定 Nginx 的发行版安装路径；首次登录后在 Web 页面以当前 Notion 实践和安装期探测值预填 Profile，管理员可修改结构化字段并提交强校验；
14. 安装后和每次发布时都确认校验命令与 reload 操作指向同一个实际运行的 Nginx，无法确认时禁止发布。

## 4. 第一版不包含

- 不管理站点证书、Cloudflare real IP 网段、`server`、`location` 或 `proxy_pass` 主配置；
- 不允许输入任意 Nginx 片段或任意正则表达式；
- 不提供一个中心服务管理多台主机，也不保存远程 SSH/root 凭据；
- 不提供规则级白名单，同一实例的全部启用规则共享一套白名单；
- 不引入数据库、消息队列、前后端分离 SPA、多用户、角色、注册或找回密码邮件体系；
- 不提供匿名管理模式；凭据缺失或损坏时服务拒绝启动；
- 不因增加密码登录就把管理页面直接暴露到公网；
- 不自动把请求来源 IP 加入白名单，避免 SSH 隧道、反向代理或错误 real IP 配置造成误判；
- 不提供项目或实例删除，第一版只支持停用实例；规则允许删除；
- 不自动改写现有业务站点配置，首次接入仍由运维人工加入固定 `include`；
- 不自动安装或升级 Nginx，不固定 Linux 发行版；第一版要求目标主机使用 systemd 管理单个本机 Nginx，不支持容器内 Nginx、非 systemd service 或同机多个独立 Nginx master。

## 5. 总体设计

### 5.1 技术选型

采用 Go 单代码库、服务端渲染页面和内部 JSON 文件存储：

- 使用 `net/http` 和 `html/template` 提供页面，不建设独立前端工程；
- 使用 `embed` 把模板和静态资源编入二进制；
- 使用 `net/netip` 解析和规范化 IP/CIDR；
- 使用 `golang.org/x/crypto/argon2` 实现管理员密码的 Argon2id 哈希；
- 编译为 Linux 单二进制，目标主机不需要安装 Go、Node.js 或 Java 运行时；
- 同一二进制通过 `serve`、`profile apply-candidate`、`publish`、`recover`、`admin` 和 `config` 子命令承担不同职责。

选择内部 JSON 而不是数据库的原因：第一版是单管理员、单机、单 Web 进程、低频配置管理，数据量小；文件锁、原子写和不可变发布快照已经能满足一致性与历史恢复要求。若后续出现多用户权限、复杂检索或集中管理多台主机，再重新评估数据库，不提前引入。

### 5.2 进程与权限拓扑

安装后的二进制由 root 所有、应用用户不可修改。常驻 Web 服务和 root 发布职责由独立 systemd unit 隔离，root 发布进程只以 oneshot 方式运行。目标 Linux 发行版不固定，但第一版以 systemd 和单个本机 Nginx service 作为部署边界。

```text
浏览器
  │ SSH 隧道访问 127.0.0.1:7582
  ▼
非 root Web 进程
  ├─ 单管理员登录、Session、CSRF
  ├─ 表单校验、草稿读写
  ├─ 配置预览与差异生成
  └─ 写入固定 Profile/发布候选文件
           │ 只能分别触发固定的 root oneshot unit
           ├──────────────────────────────┐
           ▼                              ▼
root Profile apply oneshot          root publish oneshot
  ├─ 独立复验候选 Profile             ├─ 独立复验完整规则数据
  ├─ nginx -T / systemd 强校验        ├─ 生成不可变 release
  ├─ 原子替换正式 Profile              ├─ 切换 current 并执行 nginx -T
  └─ 失败保留旧 Profile                └─ reload，失败时恢复旧版本
           ▼
         Nginx
```

systemd unit 职责：

| Unit | 身份 | 职责 |
| --- | --- | --- |
| `nginx-acl-manager.service` | 专用非 root 用户 | 常驻 Web 服务，只写规则草稿、候选 Profile 和固定请求文件 |
| `nginx-acl-manager-profile-apply.service` | root，`Type=oneshot` | 读取固定候选 Profile，强校验后原子应用正式 Profile 和 Nginx 接入 |
| `nginx-acl-manager-publish.service` | root，`Type=oneshot` | 读取固定候选文件，生成 release、校验并 reload Nginx |
| `nginx-acl-manager-recover.service` | root，`Type=oneshot` | 在 Profile 指定的 Nginx service 启动前恢复未完成发布事务 |

Web unit 只能通过 sudoers 中的精确命令分别启动固定 Profile apply 和 publish unit，不能传入 unit 名、路径、shell 文本或其他 systemctl 参数。真正的 root 操作由 PID 1 在独立 unit 中启动，因此可分别限制 Web 和 root 进程的文件系统权限。Profile apply 成功后根据已验证的 `serviceName` 渲染 recover unit 的 `Before=`，并为目标 Nginx service 创建本工具独占的 drop-in，通过 `Requires=` 和 `After=` 保证恢复成功后才能启动 Nginx；仅声明 `Before=` 但不建立拉起关系不算完成。该值只允许合法的 `.service` unit 名并保存在 root 所有的配置中。

不采用 Docker。该工具必须访问 Profile 指定的宿主机 Nginx 配置、执行校验并 reload 宿主机 service；容器化仍需挂载高权限目录和控制宿主机 systemd，权限边界更复杂。

### 5.3 部署文件

第一版发布 Linux `amd64`、`arm64` 压缩包，包含二进制、安装脚本和 systemd unit 模板。安装后主要文件如下：

```text
/usr/local/bin/nginx-acl-manager
/etc/nginx-acl-manager/
├── config.json
├── auth.json
└── nginx-profile.json
/var/lib/nginx-acl-manager/
└── staging/
    └── nginx-profile-candidate.json
/etc/nginx/access-control/
/etc/systemd/system/
├── nginx-acl-manager.service
├── nginx-acl-manager-profile-apply.service
├── nginx-acl-manager-publish.service
├── nginx-acl-manager-recover.service
└── <nginx-service>.d/
    └── 50-nginx-acl-manager-recover.conf
/etc/sudoers.d/nginx-acl-manager
```

二进制、systemd unit、Nginx service drop-in 和 sudoers 由 root 所有。drop-in 只建立 recover 依赖，不修改 Nginx 的 `ExecStart` 或 reload 实现。`config.json`、`auth.json` 和正式 `nginx-profile.json` 由 root 写入，Web 服务组只读；草稿和候选目录由专用应用用户写入。

`/etc/nginx/access-control` 是本工具拥有的固定生成目录，不是对 Nginx 安装目录的假设。即使 Nginx 安装在其他位置，也通过 Profile 中的绝对 `httpIncludeFile` 引用这个固定目录；该生成目录不提供安装参数覆盖，避免 release、回退和站点接入路径产生多套事实来源。

### 5.4 安装、Web 初始化与 Nginx Profile

第一版支持任意 Linux 发行版上的 systemd Nginx，但不尝试猜测所有发行版和源码安装布局。默认流程调整为“安装管理员和 Web 服务 → 登录后在 Web 确认 Profile → root 强校验并应用”。安装脚本不再要求 Nginx Profile 必须当场配置成功，也不在首次启动前修改现有 Nginx。

当前 Notion 实践明确记录的默认事实如下：

| 项目 | Notion 默认基线 | 页面预填与处理 |
| --- | --- | --- |
| 安装来源 | Debian 13 上通过 `apt install nginx` 安装 | 只作为已验证基线；本工具不执行 `apt`，也不据此限制发行版 |
| Nginx 命令 | `nginx` | 安装期尝试从 `PATH` 解析绝对路径，结果仅作为候选值 |
| 主配置 | Notion 未记录绝对路径 | 候选可留空或由探测结果预填；root apply 结合 `nginx -V`、`nginx -T` 确认实际值 |
| systemd service | Notion 使用 `systemctl ... nginx` | 页面默认 `nginx.service`，管理员可修改 |
| 管理器全局入口 | Notion 中尚无本工具入口 | 页面默认 `/etc/nginx/conf.d/50-nginx-acl-manager.conf`，只有确认位于实际 `http` 配置树时才应用 |
| Cloudflare real IP | `/etc/nginx/snippets/cloudflare-real-ip.conf` | 页面默认该路径；本工具只验证是否加载，不维护其内容 |

Notion 没有记录的绝对路径不得仅凭“Debian 通常如此”写入正式 Profile。安装器可以探测并预填候选值，但只有 root Profile apply 对实际 systemd service 和 `nginx -T` 完成强校验后，解析结果才能保存为正式 Profile。

默认安装命令：

```bash
sudo ./install.sh
sudo ./install.sh --port 17582
```

Web 服务固定绑定 `127.0.0.1`，默认端口为 `7582`。安装程序校验端口范围和占用情况，把端口写入 `/etc/nginx-acl-manager/config.json`，初始化单管理员后即可启动 Web，不开放防火墙。

```json
{
  "listenPort": 7582
}
```

为便于无人值守部署，原 Nginx 安装参数仍可选用，但其语义只是预填 `/var/lib/nginx-acl-manager/staging/nginx-profile-candidate.json`，不会绕过 Web 登录、root 强校验或直接修改 Nginx：

```bash
sudo ./install.sh \
  --nginx-bin /opt/nginx/sbin/nginx \
  --nginx-conf /opt/nginx/conf/nginx.conf \
  --nginx-prefix /opt/nginx \
  --nginx-service custom-nginx.service \
  --nginx-http-include /opt/nginx/conf/http.d/50-nginx-acl-manager.conf \
  --nginx-real-ip-snippet /opt/nginx/conf/snippets/cloudflare-real-ip.conf
```

首次登录以及后续修改都使用同一个“Nginx 设置”页面。单管理员登录后可进入所有管理页面，不增加角色、二次密码或额外授权；尚无正式 Profile 时页面仍可浏览，但发布和恢复操作因缺少生效前提而返回明确的业务状态错误。

页面只编辑以下结构化字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `binaryPath` | 是 | Nginx 二进制绝对路径 |
| `configPath` | 是 | Nginx 主配置绝对路径 |
| `prefixPath` | 否 | systemd `ExecStart` 使用 `-p` 或相对路径时填写 |
| `serviceName` | 是 | systemd `.service` unit 名 |
| `httpIncludeFile` | 是 | 本工具独占的 `http` 级入口文件 |
| `realIpSnippetPath` | 是 | 当前 Cloudflare real IP snippet 路径 |

点击“保存草稿”只原子更新 Web 用户可写的候选文件。点击“验证并应用”时，Web 通过 sudoers 中的精确命令启动固定 `nginx-acl-manager-profile-apply.service`；root 进程重新解析候选 JSON，执行第 9.2 节的全部强校验，并在成功后原子写入 `/etc/nginx-acl-manager/nginx-profile.json`。页面等待固定结果文件或轮询状态，只展示脱敏摘要。

正式 Profile 示例：

```json
{
  "binaryPath": "/usr/sbin/nginx",
  "configPath": "/etc/nginx/nginx.conf",
  "serviceName": "nginx.service",
  "httpIncludeFile": "/etc/nginx/conf.d/50-nginx-acl-manager.conf",
  "realIpSnippetPath": "/etc/nginx/snippets/cloudflare-real-ip.conf"
}
```

示例中的 `binaryPath` 和 `configPath` 只是 Notion Debian 13 基线可能解析出的结果，不是跨系统常量。`prefixPath` 未使用时不写入。所有路径都必须是绝对路径，经符号链接解析后指向 root 不可被 Web 用户修改的位置；`serviceName` 只接收合法 `.service` unit 名。任何进程都使用参数数组执行命令，不调用 `/bin/sh -c`，页面不提供任意 validate/reload 命令或整段 Nginx 配置输入框。

后续在 Web 修改 Profile 时仍采用候选与正式文件分离。新 Profile 通过全部强校验后，root apply 事务式替换正式文件、recover unit 和目标 service drop-in，执行 `systemctl daemon-reload` 并验证依赖关系；失败时保留旧 Profile 和本工具原有 drop-in，不能只应用部分字段。

绑定地址是固定业务常量 `127.0.0.1`，不在配置文件中重复提供可变开关。安装后修改管理端口仍使用本机命令：

```bash
sudo nginx-acl-manager config set-port 17582
```

命令先校验新端口，再原子更新配置并重启 Web unit；新端口启动失败时恢复旧配置并重新启动旧端口，同时报告原因。修改的是管理页面端口，不影响业务实例的 `localPort`。

### 5.5 单管理员认证

第一版只有一个管理员账号，不设计用户列表、角色、注册、邀请、邮件找回或管理员删除。除 `/login` 和不返回敏感数据的 `/healthz` 外，所有页面和操作都要求有效登录 Session。

安装程序必须在启用 Web unit 前，通过二进制直接读取 TTY，依次提示管理员用户名、密码和确认密码。密码输入不回显，不进入命令参数、环境变量、shell 变量、安装日志或 systemd journal；不提供默认用户名或默认密码。初始化失败时不得启动服务。

凭据保存在 `/etc/nginx-acl-manager/auth.json`：

```json
{
  "username": "admin",
  "passwordHash": "$argon2id$..."
}
```

示例用户名只说明结构，安装时不自动采用。文件权限为 `root:<应用组> 0640`，Web 进程只读，凭据文件不进入项目草稿、release、配置 diff 或备份快照。

密码使用 Argon2id 和独立的 16 字节加密安全随机盐，输出 32 字节摘要，PHC 字符串中保存算法和参数。第一版固定参数为 `memory=19456 KiB`、`iterations=2`、`parallelism=1`。加载凭据时只接受该固定参数组合，避免损坏文件触发异常资源消耗。不保存明文、可逆密文或与哈希同文件的 pepper。

由于第一版不提供 MFA，密码至少 15 个字符；不设置大小写、数字或特殊字符组合规则，允许 Unicode 和空格，支持至少 64 个字符，绝不静默截断。登录错误统一返回“用户名或密码错误”，并按账号做渐进式失败延迟，避免通过响应枚举用户名或高速尝试密码。

账号维护命令：

```bash
sudo nginx-acl-manager admin set-username
sudo nginx-acl-manager admin set-password
sudo nginx-acl-manager admin reset
```

用户名和密码均通过 TTY 交互读取；密码不允许通过 `--password`、环境变量或标准命令参数传递。命令以 root 身份校验输入、原子替换 `auth.json` 并重启 Web unit，使全部既有 Session 立即失效。拥有主机 sudo 权限是忘记密码时的恢复边界。

登录成功后生成不可预测的随机 Session ID，服务端 Session 保存在 Web 进程内存；Cookie 只保存 Session ID，并设置 `HttpOnly`、`SameSite=Strict`。登录后重新生成 Session ID，Session 同时具有空闲过期和绝对过期；服务重启、用户名变更或密码变更都会使全部 Session 失效。所有写操作和登录表单都使用 CSRF 防护。

## 6. 领域模型

### 6.1 数据层级

```text
项目 Project
└── 服务实例 Instance
    ├── 白名单 AllowlistEntry
    └── 受保护接口 Rule
```

### 6.2 项目

| 字段 | 说明 |
| --- | --- |
| `slug` | 稳定项目标识，创建后不可修改，用于目录和变量前缀 |
| `displayName` | 页面展示名称，可修改 |
| `instances` | 项目下的实例列表 |

`slug` 只允许小写字母、数字和连字符，且以小写字母开头。同一主机内必须唯一。

### 6.3 服务实例

| 字段 | 说明 |
| --- | --- |
| `key` | 项目内稳定实例标识，创建后不可修改 |
| `displayName` | 实例展示名称，如“主实例”“测试实例” |
| `enabled` | 是否参与 Nginx 配置生成 |
| `localPort` | 本地服务端口，用于页面识别和生成接入提示 |
| `serverName` | 可选入口域名，仅用于展示和接入提示，不参与 ACL 匹配 |
| `denyStatus` | 拒绝状态，只允许 `403` 或 `404` |
| `allowedCidrs` | 实例共享的白名单 |
| `rules` | 该实例的受保护接口规则 |

第一版本地地址是固定业务规则 `127.0.0.1`，不存储为可配置字段。本地端口必须在 `1` 到 `65535` 范围内。

实例目录只使用稳定 `key`，不把端口写入目录名。这样修改 `localPort` 时，规则和站点中的 ACL `include` 路径保持不变。管理工具不修改 `proxy_pass`；如果实例端口实际发生变化，运维仍需同步调整业务站点的反代目标。

### 6.4 白名单条目

| 字段 | 说明 |
| --- | --- |
| `id` | 系统生成的稳定条目标识 |
| `cidr` | 规范化后的 IPv4/IPv6 CIDR |
| `label` | 用途说明，如“家庭出口”“办公出口” |

第一版支持新增、编辑和删除白名单，不增加单独的启停状态。临时取消某个地址时从草稿删除即可，已发布版本仍可用于恢复。

### 6.5 接口规则

| 字段 | 说明 |
| --- | --- |
| `id` | 系统生成的稳定规则标识 |
| `name` | 规则名称 |
| `enabled` | 是否参与配置生成 |
| `methods` | 精确 HTTP 方法集合 |
| `path.type` | `exact` 或 `numeric_segment` |
| `path.value` | 绝对路径或包含 `{id}` 的数字路径模板 |
| `path.optionalTrailingSlash` | 是否同时匹配尾部 `/` |

新增规则固定默认为停用。启用、停用和删除均只修改草稿，发布后才生效。删除采用物理移除，不增加软删除字段；历史发布快照负责恢复能力。

方法按用户选择精确匹配，不隐式把 `GET` 扩展为 `HEAD`。第一版方法枚举为 `GET`、`HEAD`、`POST`、`PUT`、`PATCH`、`DELETE` 和 `OPTIONS`。

### 6.6 内部 JSON 示例

该文件只由应用读写，页面不向用户暴露源码。

```json
{
  "slug": "sub2api",
  "displayName": "Sub2API",
  "instances": [
    {
      "key": "main",
      "displayName": "主实例",
      "enabled": true,
      "localPort": 7777,
      "serverName": "sub2api.example.com",
      "denyStatus": 404,
      "allowedCidrs": [
        {
          "id": "<系统生成ID>",
          "cidr": "203.0.113.10/32",
          "label": "可信出口"
        }
      ],
      "rules": [
        {
          "id": "<系统生成ID>",
          "name": "保存系统设置",
          "enabled": true,
          "methods": ["PUT"],
          "path": {
            "type": "exact",
            "value": "/api/v1/admin/settings",
            "optionalTrailingSlash": true
          }
        }
      ]
    }
  ]
}
```

## 7. 输入与业务校验

### 7.1 标识和基础字段

- `slug`、实例 `key` 必须符合 `[a-z][a-z0-9-]*`，并在各自作用域唯一；
- 稳定标识创建后不可修改，展示名可修改；
- `localPort` 必须是有效 TCP 端口；
- `serverName` 如填写，只接收主机名，不接收协议、端口、路径或查询串；
- `denyStatus` 必须由 `403/404` 枚举选择，不接收任意状态码；
- 所有展示内容在服务端模板输出时做 HTML 转义。

### 7.2 IP/CIDR 规范化

使用 Go `net/netip` 解析并保存规范形式：

| 用户输入 | 内部保存 | 语义 |
| --- | --- | --- |
| `203.0.113.10` | `203.0.113.10/32` | 单个 IPv4 |
| `203.0.113.0/24` | `203.0.113.0/24` | IPv4 网段 |
| `2001:db8::1` | `2001:db8::1/128` | 单个 IPv6 |
| `2001:db8::/32` | `2001:db8::/32` | IPv6 网段 |
| `0.0.0.0` | `0.0.0.0/0` | 放行全部 IPv4 |
| `::/0` | `::/0` | 放行全部 IPv6 |

只有不带掩码的 `0.0.0.0` 使用上述特殊含义；显式输入的合法 CIDR 按其 CIDR 语义处理。相同实例内不允许保存重复的规范化 CIDR。

当存在 `0.0.0.0/0` 时，页面和发布确认区必须显示高风险提示：“全部 IPv4 来源均可访问已启用的受保护接口”。其他 IPv4 条目即使被覆盖也不自动删除。`0.0.0.0/0` 不放行 IPv6；需要全部放行 IPv6 时必须单独添加 `::/0`。

当实例存在启用规则但白名单为空时，语义是拒绝所有来源访问这些规则，而不是跳过保护。预览和发布页必须明确提示这一点。

### 7.3 路径规则

- 路径必须以 `/` 开始，不接收 scheme、host、查询串或 fragment；
- 禁止控制字符、换行和分号；
- `exact` 将路径所有正则元字符转义后生成精确正则；
- `numeric_segment` 只允许完整路径段 `{id}`，生成 `[0-9]+`，不接受用户自定义正则；
- `optionalTrailingSlash=false` 时只匹配表单路径本身；为 `true` 时同时匹配一个尾部 `/`；
- 匹配使用 Nginx `$uri`，因此忽略查询参数，并遵循 Nginx 的 URI 规范化结果；
- 同一实例内完全相同的方法、路径类型、路径值和尾部斜杠语义只生成一个有效匹配项，页面提示重复规则。

## 8. 草稿与发布数据

### 8.1 文件布局

```text
/var/lib/nginx-acl-manager/
├── drafts/
│   └── projects/
│       ├── sub2api.json
│       └── cliproxyapi.json
└── staging/
    └── candidate.json

/etc/nginx/access-control/
├── current -> releases/<REVISION>
└── releases/
    └── <REVISION>/
        ├── manifest.json
        └── projects/
            └── sub2api/
                ├── source.json
                └── instances/
                    └── main/
                        ├── http/
                        │   ├── 10-allowlist.conf
                        │   └── 20-routes.conf
                        └── location/
                            └── 10-enforce.conf
```

### 8.2 数据职责

- `drafts/projects/*.json`：当前可编辑草稿，由非 root Web 进程所有；
- `staging/candidate.json`：一次发布的完整候选状态，应用以临时文件加原子重命名写入；
- `releases/<REVISION>/projects/*/source.json`：该发布版本使用的项目快照；
- `releases/<REVISION>/**/*.conf`：由快照确定性生成的 Nginx 文件；
- `manifest.json`：发布编号、创建时间、上一版本、变更项目、动作类型、文件摘要和验证结果；
- `current`：唯一生效版本指针，也是“当前已发布数据”的事实来源。

草稿文件每次修改都在进程内加项目锁，重新读取最新草稿、应用单项变更，再通过同目录临时文件、`fsync` 和原子重命名落盘，避免两个表单请求覆盖整个旧页面快照。不引入额外的数据版本字段。

release 创建后不可修改，第一版不自动清理历史版本，也不在日志中打印完整白名单、候选 JSON 或生成配置。

## 9. Nginx 配置生成

### 9.1 生效前提

生成 `/etc/nginx/access-control/` 下的文件不代表规则已经生效。完整链路必须同时满足：

```text
可信代理恢复真实客户端 IP
  → http 块加载实例 geo/map
  → 业务 location 加载实例 enforce 文件
  → current 指向已验证 release
  → Nginx reload 成功
```

缺少全局 `http` include 时，白名单和路由变量不会进入配置；缺少实例 `location` include 时，不会执行拒绝；缺少 real IP 配置时，`geo` 可能匹配 Cloudflare 节点 IP；未 reload 时，运行中的 worker 继续使用旧配置。因此安装、实例接入和发布都必须有实际配置树校验，不能只检查文件是否存在。

### 9.2 Web 候选 Profile 的 root 强校验与全局接入

Profile apply 进程把 Nginx 视为主机前置条件，不自动安装或升级。Web 登录只授予页面操作权限，root 进程仍必须把候选数据视为不可信输入。强校验分为“只读确认实际运行对象”和“事务式接入全局 include”两段；任何一段失败都不得替换正式 Profile。

只读确认必须按以下顺序完成：

1. 从固定候选文件读取 `binaryPath`、`configPath`、可选 `prefixPath`、`serviceName`、`httpIncludeFile` 和 `realIpSnippetPath`，重新执行完整结构和字段校验；
2. 确认二进制和配置路径存在、类型正确，规范化后的目标不可由 Web 应用用户或应用组写入；
3. 使用 `systemctl show` 读取目标 service 的 `LoadState`、`ActiveState`、`FragmentPath`、`ExecStart` 和 `MainPID`，确认 unit 已加载且运行中；
4. 将 unit 的 `ExecStart` 与候选 Profile 交叉比对，确认实际 service 使用同一个规范化 Nginx 二进制、主配置和可选 prefix；如果 unit 通过无法可靠解析的 shell/wrapper 启动、同机存在多个候选 master，或参数互相矛盾，应用立即失败并在页面提示管理员调整字段或 service；
5. 以参数数组执行 `<binaryPath> [-p <prefixPath>] -T -c <configPath>`，确认输出中的顶层配置就是 `configPath`，且语法测试成功；不得因为另一个 `nginx` 命令测试成功就接受当前 Profile；
6. 对 Cloudflare 基线，确认 `realIpSnippetPath` 被同一份展开配置加载，并存在预期的 `real_ip_header CF-Connecting-IP` 和可信代理配置；无法确认时阻止规则发布。本工具只验证，不更新 Cloudflare 网段。

Profile 只读确认通过后，root apply 在缺少初始版本时创建有效的初始 release 和 `current`，再事务式创建候选 Profile 指定的 `httpIncludeFile`。默认候选路径是 `/etc/nginx/conf.d/50-nginx-acl-manager.conf`，但它不是跨发行版假设；只有同一 Profile 的展开配置能够实际加载该文件时才成立。文件内容只有本工具标记和固定全局入口：

```nginx
# managed-by: nginx-acl-manager; scope=http; version=1
include /etc/nginx/access-control/current/projects/*/instances/*/http/*.conf;
```

接入校验必须确认：

1. `nginx -T` 展开结果中出现了这份带管理标记的确切文件，不能只检查文件已写入磁盘；
2. root apply 使用临时、无副作用的 `map` 探针验证入口处于 `http` 上下文，探针只参与候选配置测试，不进入最终 release，也不触发 reload；
3. 清除探针后再次执行完整 `nginx -T`，最终配置仍成功；
4. 目标文件若已存在但不带本工具管理标记，不覆盖、不合并，要求用户另选 `--nginx-http-include`；
5. 目标配置未加载该路径时，不猜测其他目录、不重写整个 `nginx.conf`，而是回滚本次新增文件和初始指针，保留旧正式 Profile，并在固定结果文件中给出需要调整的字段和精确 `include`。

应用阶段不因写入全局入口就立即认可新 Profile；只有强校验全部通过且最终配置无探针后，才 reload 候选 Profile 指定的 service，并再次检查 service 为 active、同一 Profile 的 `nginx -T` 成功。成功后原子替换正式 Profile；失败时只删除本次带管理标记的新文件并恢复旧的本工具文件，绝不删除或覆盖用户已有 Nginx 配置。

### 9.3 项目实例接入与校验

项目和实例可能在基础安装后才创建，且工具不能安全推断任意业务站点中哪个 `location` 对应哪个本地端口，因此不自动解析或改写现有业务站点文件。项目页面为每个实例生成稳定接入片段：

```nginx
location / {
    include /etc/nginx/access-control/current/projects/sub2api/instances/main/location/*.conf;

    proxy_pass http://127.0.0.1:7777;
}
```

运维只需在对应业务 `location` 中加入一次 `include`，后续规则变化不再修改站点文件。生成的 http 和 location 文件都带有项目、实例的固定标记注释。

发布启用实例时，root 发布进程在候选版本切换到 `current` 后，先重新执行第 9.2 节的 service/Profile 一致性检查，再以 Profile 的绝对路径和参数执行 `nginx -T`，只在内存中检查展开结果，确认：

1. 目标实例的 `http` 文件已通过全局入口加载；
2. 目标实例的 `location` 文件已在业务站点中加载；
3. 展开结果仍来自 Profile 指定的主配置，且配置测试成功；
4. 当前 systemd service 的实际启动对象仍与安装时保存的 Profile 一致。

任一条件不满足时，恢复旧 `current`、不 reload，并返回“实例尚未完成 Nginx 接入”。完整 `nginx -T` 输出不得写入普通日志。停用实例可以作为草稿或无副作用 release 保留；实例从停用改为启用时必须重新通过上述检查。

当前手工 Sub2API ACL 不由安装程序自动删除。首次迁移先让生成配置通过接入验证和端到端测试，再在单独的上线步骤中移除旧片段，避免安装过程覆盖现有保护规则。

### 9.4 变量命名

变量按项目和实例隔离：

```text
$acl_<project_slug>_<instance_key>_ip_allowed
$acl_<project_slug>_<instance_key>_route_protected
$acl_<project_slug>_<instance_key>_blocked
```

生成变量时把标识中的 `-` 转为 `_`。因为输入标识不允许 `_`，不同合法标识不会在该转换中发生碰撞。

### 9.5 生成示例

`http/10-allowlist.conf`：

```nginx
geo $acl_sub2api_main_ip_allowed {
    default          0;
    203.0.113.10/32  1;
}
```

`http/20-routes.conf`：

```nginx
map "$request_method:$uri" $acl_sub2api_main_route_protected {
    default                                            0;
    "~^PUT:/api/v1/admin/settings/?$"                 1;
    "~^GET:/api/v1/admin/accounts/data/?$"            1;
    "~^HEAD:/api/v1/admin/accounts/data/?$"           1;
    "~^POST:/api/v1/user/totp/disable/?$"             1;
    "~^PUT:/api/v1/admin/accounts/[0-9]+/?$"           1;
}

map "$acl_sub2api_main_route_protected:$acl_sub2api_main_ip_allowed" $acl_sub2api_main_blocked {
    default  0;
    "1:0"    1;
}
```

`location/10-enforce.conf`：

```nginx
if ($acl_sub2api_main_blocked) {
    return 404;
}
```

此处 `if` 内只执行 `return`，不放置代理、文件或重写逻辑。

生成器必须满足以下规则：

- 只输出已启用实例中的已启用规则；
- 实例停用或全部规则停用时保留目录和文件，但生成无副作用的注释文件；
- 白名单为空时仍生成 `default 0`，使启用规则拒绝所有来源；
- `0.0.0.0/0` 和 `::/0` 按独立地址族写入 `geo`；
- 项目、实例、白名单和规则使用稳定排序，确保相同输入产生逐字节相同输出；
- 所有路径正则由受控模板编译，不拼接用户提供的 Nginx 指令；
- 每个 release 生成全部已发布项目，避免通配 include 混用新旧版本文件。

## 10. 页面与操作流程

### 10.1 页面结构

```text
登录页
└── 单管理员用户名和密码

Nginx 设置
├── 正式 Profile 与候选差异
├── 二进制、主配置、prefix、service、全局入口、real IP snippet
└── 保存草稿、验证并应用、最近应用结果

项目列表
└── 项目详情
    ├── 当前发布版本、草稿状态、Nginx 最近发布结果
    ├── 实例切换（名称、127.0.0.1:端口、入口域名、状态）
    └── 实例详情
        ├── 基础设置
        ├── 白名单表格
        ├── 受保护接口规则表格
        ├── 配置预览与差异
        └── 发布与历史版本
```

规则列表展示实例和端口、状态、名称、方法、路径及操作：

| 状态 | 实例/端口 | 方法 | 路径 | 操作 |
| --- | --- | --- | --- | --- |
| 已启用 | 主实例 / 7777 | PUT | `/api/v1/admin/settings` | 编辑、停用、删除 |
| 已停用 | 主实例 / 7777 | POST | `/api/v1/user/totp/disable` | 编辑、启用、删除 |

删除规则前由页面确认；后端仍按稳定规则 ID 定位并修改草稿。按钮显隐和确认弹窗属于页面交互，最终状态变更由后端重新校验。

### 10.2 服务端路由

第一版使用 HTML 表单 POST 和 `303` 重定向，不额外提供公共 JSON API：

| 路由 | 用途 |
| --- | --- |
| `GET /login` | 管理员登录页 |
| `POST /login` | 校验管理员凭据并创建 Session |
| `POST /logout` | 销毁当前 Session |
| `GET /` | 项目列表和运行状态 |
| `GET /settings/nginx` | 查看正式/候选 Nginx Profile 和最近应用结果 |
| `POST /settings/nginx` | 校验基础字段并保存候选 Profile |
| `POST /settings/nginx/apply` | 触发固定 root Profile apply unit |
| `GET /projects/{slug}` | 项目及实例详情 |
| `POST /projects` | 新增项目草稿 |
| `POST /projects/{slug}` | 修改项目展示信息 |
| `POST /projects/{slug}/instances` | 新增实例 |
| `POST /projects/{slug}/instances/{key}` | 修改实例 |
| `POST /projects/{slug}/instances/{key}/allowlist` | 新增白名单条目 |
| `POST /projects/{slug}/instances/{key}/allowlist/{id}` | 编辑或删除白名单条目 |
| `POST /projects/{slug}/instances/{key}/rules` | 新增规则 |
| `POST /projects/{slug}/instances/{key}/rules/{id}` | 编辑、启停或删除规则 |
| `POST /projects/{slug}/preview` | 生成当前草稿预览和差异 |
| `POST /projects/{slug}/publish` | 发布当前项目草稿 |
| `POST /projects/{slug}/restore/{revision}` | 以历史项目快照创建新发布版本 |
| `GET /healthz` | Web 进程存活检查，不检查或修改 Nginx |

除 `/login` 和 `/healthz` 外，所有路由都必须先校验 Session；单管理员登录后可访问全部设置和业务页面，不再增加角色或二次授权。`/healthz` 不返回配置、账号或 Nginx 状态。所有 POST（包括登录）必须使用 CSRF 令牌；Cookie 使用 `HttpOnly`、`SameSite=Strict`，在 HTTPS 场景同时使用 `Secure`。管理页面默认监听 `127.0.0.1:7582`，安装覆盖端口后按实际端口校验允许的 `Host`。模板输出统一转义，错误页不回显候选文件内容或内部命令输出。

## 11. Nginx Profile、预览、发布与恢复

### 11.1 Nginx Profile Web 应用

1. 登录管理员在设置页编辑结构化字段并保存候选 Profile；
2. Web 层只做表单必填、绝对路径和 service 名格式校验，原子写入固定候选文件；
3. 管理员点击“验证并应用”，Web 触发固定 `nginx-acl-manager-profile-apply.service`，不向 unit 传递表单字段或路径；
4. root apply 从固定路径重新读取候选数据，执行第 9.2 节全部校验、接入、reload 和回退流程；
5. root apply 将成功或失败摘要写入固定结果文件，Web 页面读取后展示；
6. 成功时显示新的正式 Profile 和差异，失败时继续显示旧正式 Profile，并保留候选值供管理员修正。

首次没有正式 Profile 时不隐藏项目页面，但任何发布、恢复或 Nginx 状态检测都返回“请先验证并应用 Nginx 设置”。这是生效前提校验，不是管理员权限限制。

### 11.2 预览

预览某个项目时：

1. 读取 `current` 中所有已发布项目快照；
2. 只用目标项目的当前草稿替换其已发布快照；
3. 不带入其他项目尚未发布的草稿；
4. 确定性生成完整候选文件集；
5. 展示字段级风险提示、生成文件和相对当前 release 的统一差异。

这样发布 Sub2API 不会顺带发布 CLIProxyAPI 的未完成草稿。

### 11.3 发布

发布是全局 release、项目级变更，流程如下：

1. Web 进程获取发布互斥锁并固定目标项目草稿快照；
2. 按预览规则组装全部项目的候选状态；
3. 将完整候选 JSON 原子写入固定 `staging/candidate.json`；
4. Web 进程通过 sudoers 中的精确命令启动固定 `nginx-acl-manager-publish.service`，不传入 unit 名、路径、shell 文本或 Nginx 片段；
5. systemd 以 root 身份启动 oneshot 发布进程，重新解析并校验全部候选数据，同时读取 root 所有的 Nginx Profile，不信任 Web 层校验结果或命令参数；
6. 在 release 根目录生成临时版本，写入 source、manifest 和全部 `.conf`，完成后原子改名为不可变版本；
7. 记录旧、新 revision 和阶段到 root 所有的事务标记；
8. 重新比对 systemd service 与 Nginx Profile，原子切换 `current` 到候选版本，再执行 `<binaryPath> [-p <prefixPath>] -T -c <configPath>`，同时完成语法测试和全局/实例 include 展开校验；
9. 语法或接入校验失败时恢复旧 `current`，不 reload，并保存脱敏错误摘要；
10. 校验成功后以参数数组执行 `systemctl reload <serviceName>`，`serviceName` 只能来自 root 所有且已验证的 Profile；
11. reload 失败时恢复旧 `current`，验证旧配置并重新加载旧版本；
12. 成功后清除事务标记，释放锁并向页面返回新 revision。

之所以在 `nginx -T` 前短暂切换 `current`，是因为现有站点通过绝对 `current` 路径 include，完整测试必须让实际运行的 Nginx 解析候选文件。运行中的 worker 在 reload 前仍使用旧配置。为覆盖进程被强杀或主机断电的窗口，发布命令每次启动都先检查未完成事务；安装器按 Profile 的 `serviceName` 渲染 root recover oneshot，并通过目标 service drop-in 的 `Requires=`、`After=` 拉起它，发现未完成事务时先把 `current` 恢复到旧 revision。

发布锁必须覆盖发布和恢复操作。Web 层只能启动固定 publish unit；root 发布进程自行打开固定候选文件、固定 release 根目录和 root 所有的 Nginx Profile。Profile 只描述路径、可选 prefix 和 service unit，不包含可执行 shell 文本。

### 11.4 历史恢复

release 是全局快照，但页面按项目恢复：

1. 从所选历史 revision 读取目标项目 `source.json`；
2. 保留当前 revision 中其他项目的快照；
3. 组装为新的完整候选状态；
4. 走与普通发布完全相同的校验、生成、`nginx -T`、接入检查、切换和 reload 流程；
5. 创建新的 revision，不直接修改或重新激活旧目录。

因此恢复 Sub2API 不会回退其他项目。被删除规则可从历史项目快照恢复；当前草稿不自动覆盖，页面在恢复前展示差异并确认。

## 12. 权限与安全边界

- Web 服务使用专用非 root 用户，默认仅监听 `127.0.0.1:7582`，推荐通过 SSH 隧道访问；
- 第一版只允许单管理员登录，不存在匿名管理、注册、第二管理员或角色绕过路径；
- 管理页面不放在它自己管理的业务 ACL 后面，避免错误白名单导致无法进入管理页；
- 即使启用密码登录，管理页面也不直接暴露公网；密码和 Session 只通过 SSH 隧道、Tailscale 或 HTTPS 等强传输承载；
- 安装后的二进制、systemd unit、sudoers、认证文件和恢复服务均由 root 所有，应用用户不可修改；
- 正式 `nginx-profile.json` 仅 root 可替换，Web 用户只读；页面编辑的是固定候选文件，不能直接修改正式 Profile；
- sudoers 只允许 Web 用户启动固定 `nginx-acl-manager-profile-apply.service` 和 `nginx-acl-manager-publish.service`，不允许传入其他 unit、任意 shell、任意文件路径或任意 systemctl 参数；
- root Profile apply 和发布器只通过参数数组调用 Profile 中的绝对二进制和合法 `.service` unit，不提供任意校验命令、reload 命令、wrapper 或整段 Nginx 配置输入；
- root 发布进程不采用 Web 进程生成的 `.conf`，而是从结构化候选数据重新校验并生成；
- `drafts`、`staging` 仅应用用户可写；正式 Profile、`releases` 和 `current` 仅 root 可写；Web 用户只读正式 Profile、当前 release 的 manifest 和 source；
- `auth.json` 仅 root 可写，Web 用户只读；密码只保存 Argon2id 哈希，不进入日志、命令参数、环境变量、草稿或 release；
- 禁止路径穿越、换行注入、任意正则、任意状态码和任意 Nginx 指令；
- real IP 信任网段继续由独立 root 配置维护，本工具不允许页面修改；
- 日志记录 revision、项目、动作、阶段、退出状态和脱敏错误摘要，不记录凭据，不默认打印完整 IP 白名单或请求文件；
- 页面所有写操作使用 POST 和 CSRF 防护，服务端不依据前端按钮状态跳过业务校验。

## 13. 异常处理

| 场景 | 处理 |
| --- | --- |
| 管理员凭据缺失或损坏 | Web 服务拒绝启动，提示使用本机 `admin reset` 恢复 |
| 监听端口非法或已占用 | 安装/配置命令拒绝变更，Web 服务不带错误配置启动 |
| 正式 Nginx Profile 尚不存在 | Web 和登录正常运行，发布/恢复被阻止并引导进入 Nginx 设置页 |
| 候选 Profile 基础字段不合法 | 不写候选文件，页面返回对应字段错误 |
| Nginx Profile 不能唯一解析或与 systemd `ExecStart` 不一致 | Profile apply 失败并保留旧正式 Profile；发布阶段发现漂移时保持旧 `current` 并阻止 reload |
| `httpIncludeFile` 未被实际 `http` 配置树加载 | 回滚本次带管理标记的新文件，Profile apply 失败并输出调整提示 |
| 目标 include 文件已存在但不归本工具管理 | 不覆盖，要求指定其他 `--nginx-http-include` 路径 |
| 登录失败 | 返回统一错误，记录不含密码的失败事件并施加渐进延迟 |
| 草稿字段不合法 | 拒绝本次变更，返回字段错误，不改草稿 |
| 草稿原子写失败 | 保留旧草稿，记录错误并提示重试 |
| 候选完整校验失败 | 不创建可用 release，不切换 `current` |
| release 生成失败 | 删除未完成的临时目录，当前版本不变 |
| `nginx -T` 语法检查失败 | 恢复旧软链接，不 reload |
| 启用实例未被配置树加载 | 恢复旧软链接，不 reload，提示需要加入全局或实例 include |
| reload 失败 | 恢复旧软链接并重新加载旧配置 |
| 发布进程异常退出 | 下次发布前或 Nginx 启动前依据事务标记恢复旧 revision |
| 某项目没有启用实例 | 保留项目快照，生成文件不产生访问控制效果 |
| 实例有规则但全部停用 | 保留实例目录，生成无副作用文件 |
| 白名单为空且有启用规则 | 所有来源命中规则时均被拒绝，并在发布前强提示 |

完整诊断文本保存在 root 可读的受限发布结果文件中，不在应用层任意截断；systemd journal 和页面只记录安全摘要及可用于排查的 revision，避免把完整候选数据或白名单带入常规日志。

## 14. 实现边界

建议代码按真实职责划分：

```text
cmd/
└── main                 # serve、publish、recover、admin、config 子命令入口
internal/
├── auth/                # 单管理员凭据、登录校验和内存 Session
├── serverconfig/        # 监听配置读取、校验和原子更新
├── nginxprofile/        # 候选/正式 Profile、字段校验、systemd 一致性检查和 root apply
├── model/               # 项目、实例、白名单、规则模型
├── validation/          # 表单与发布侧共享的纯校验
├── draft/               # 文件锁、草稿原子读写
├── generator/           # 模型到确定性 Nginx 文件集
├── release/             # 候选合并、manifest、发布与恢复事务
└── web/                 # 路由、表单、模板视图模型
web/
├── templates/
└── assets/
```

边界说明：

- `auth` 只处理单管理员凭据和 Session，不引入通用用户、角色或权限框架；
- `serverconfig` 在服务启动时一次校验监听配置，业务请求不重复检查基础配置；
- `nginxprofile` 管理候选与正式 Profile，Web 侧只执行基础字段校验，root apply 负责解析默认值、交叉校验实际 systemd Nginx、事务式应用并返回参数数组；不接受 shell 文本；
- `generator` 只接收已解析模型并返回内存文件集，不读写固定系统目录、不执行命令；
- `release` 由 root oneshot 调用，负责文件系统事务和 Nginx 命令，不包含页面逻辑；
- `web` 先校验登录和 CSRF，只操作规则草稿、候选 Profile，并触发固定 Profile apply/publish unit，不直接写 `/etc/nginx` 或正式 Profile；
- 简单字段赋值、结果包装和一次性表单转换就地实现，不为每个操作创建无意义包装层；
- Web 预览与 root 发布复用同一校验和生成代码，但 root 入口必须重新执行，不能信任预览结果。

## 15. 验证方案

### 15.1 单元测试

- 默认监听端口 `7582`、安装覆盖端口、非法端口和配置原子回退；
- Notion 默认基线下的 `nginx` 命令解析、主配置识别和 `nginx.service` 默认值；
- Web Profile 表单和可选安装期预填参数的结构化覆盖、规范化与非法输入拒绝；
- 候选 Profile 原子保存、正式 Profile 只读、root apply 成功替换和失败保留旧值；
- 登录管理员可访问 Nginx 设置页，不存在额外角色或二次授权；未配置正式 Profile 时发布返回业务状态错误；
- systemd `ExecStart` 与 Profile 一致、不一致、不可解析 wrapper 和 Profile 漂移分支；
- 命令始终以参数数组组装，路径或 service 值不能注入额外 shell、unit 或 Nginx 参数；
- Argon2id 哈希生成、正确/错误密码校验和损坏哈希拒绝启动；
- 单管理员用户名校验、凭据原子替换和 CLI 不接收明文密码参数；
- 登录后 Session ID 轮换、空闲/绝对过期、服务重启和凭据变更失效；
- 未登录路由保护、统一登录错误、渐进延迟和 CSRF 校验；
- IPv4、IPv6、单 IP、CIDR 和非法输入解析；
- `0.0.0.0` 到 `0.0.0.0/0` 的特殊规范化；
- `0.0.0.0/0` 不覆盖 IPv6 的业务提示；
- exact 和 numeric segment 路径编译、正则转义、尾部斜杠语义；
- HTTP 方法精确匹配，不自动扩展 `GET/HEAD`；
- 停用实例、停用规则、删除规则的生成结果；
- 项目和实例变量、目录隔离，连字符转下划线无碰撞；
- 确定性排序，同一模型重复生成结果完全相同；
- 发布一个项目时不带入其他项目草稿；
- 项目级历史恢复保留其他项目当前快照；
- 草稿原子写和同项目并发修改不覆盖其他条目；
- `nginx -T`、实例接入检查、reload、恢复各失败分支的状态转换。

### 15.2 集成测试

- 验证安装不指定端口时监听 `127.0.0.1:7582`，指定端口时只使用安装值；
- 验证没有正式 Profile 时可正常登录和保存候选配置，但不能发布或恢复；
- 在 Debian `apt` 布局中验证 Web 预填默认值、保存候选并经固定 root apply unit 解析实际 Profile，不依赖代码内硬编码绝对路径；
- 在隔离环境通过 Web 设置非默认二进制、主配置、prefix、include 目录和自定义 systemd service，验证强校验后仍可发布与恢复；
- 准备第二套可通过语法测试但不由目标 service 使用的 Nginx 配置，验证强校验拒绝“测的是 A、reload 的是 B”；
- 验证默认 `conf.d` 未加载、service 不匹配、目标 include 文件已被用户占用时 Profile apply 失败且原配置不变；
- 验证凭据和配置文件权限、非 root Web unit 与 root Profile apply/publish oneshot 的身份边界；
- 用生成的完整候选 release 执行真实 `nginx -T`；
- 验证全局 `http` include 和实例 `location` include 均被 `nginx -T` 展开；
- 分别移除全局和实例 include，验证启用实例发布被阻止且旧版本不变；
- 在测试 Nginx 中验证白名单允许、非白名单拒绝、非保护 URL 不受影响；
- 验证同一项目两个实例的变量和规则互不影响；
- 验证 `0.0.0.0/0` 放行所有 IPv4，但 IPv6 仍需 `::/0`；
- 模拟 `nginx -T` 失败、reload 失败和未完成事务恢复；
- 修改 systemd unit 或 Nginx 启动参数模拟安装后 Profile 漂移，验证发布被阻止；
- 验证 recover unit 在默认和自定义 Nginx service 前都能恢复未完成事务；
- 模拟 Profile reconfigure 或 `daemon-reload` 后验证失败，确认旧 Profile 和本工具 drop-in 均被恢复；
- 检查 Web 用户不能修改 Profile、release、current、sudoers、二进制、systemd unit 和 Nginx service drop-in。

### 15.3 首次接入验收

1. 默认安装后 Web 服务只监听 `127.0.0.1:7582`；安装指定其他端口时使用指定值；
2. 安装过程必须设置唯一管理员用户名和密码，未登录访问管理页时跳转登录页；
3. 登录成功后可访问全部管理页面，错误用户名与错误密码显示相同提示，不增加角色或二次授权；
4. CLI 修改用户名或密码后，旧凭据和全部旧 Session 立即失效；
5. 首次没有正式 Profile 时，Nginx 设置页展示 Notion 默认值和探测候选；项目页面可访问，但发布和恢复明确提示先完成设置；
6. 保存 Nginx 设置只更新候选文件，不修改正式 Profile、Nginx 文件或 systemd；
7. 点击“验证并应用”后，固定 root unit 以 Notion 默认基线解析当前主机实际二进制、主配置、`nginx.service`、全局入口和 real IP snippet，并把强校验后的绝对值写入正式 Profile；
8. 在非默认 Nginx 布局中，通过 Web 修改路径、prefix、service 和全局入口后能够完成相同强校验，不要求更换 Linux 发行版；
9. Nginx service/Profile 不一致、默认 `conf.d` 未加载或目标 include 文件被占用时，应用明确失败，旧 Profile、已有 Nginx 配置和线上 worker 不变；
10. Nginx 全局入口和实例入口都能在同一 Profile 的 `nginx -T` 中确认，缺少任一入口时启用实例发布失败；
11. Sub2API 主实例配置为 `127.0.0.1:7777`，导入现有四条规则；
12. 使用脱敏外的真实白名单值通过页面录入，不写入仓库或测试快照；
13. 非白名单来源访问四条敏感接口返回所选 `404`；
14. 白名单来源能够进入原业务处理流程；
15. 非保护 URL 在白名单内外行为与接入前一致；
16. 停用某条规则并发布后，该规则不再拦截，重新启用后恢复；
17. 删除规则并发布后不再生成，按项目恢复历史版本后重新生效；
18. 新增同项目第二实例后，两套白名单和 URL 规则互不串用；
19. 错误配置导致 `nginx -T` 失败时，线上当前版本和运行中 worker 不变；
20. 发布历史能显示 revision、变更项目、结果和配置差异。

## 16. 实施顺序

1. 实现监听配置、单管理员凭据、登录 Session 和本机维护命令；
2. 实现 Nginx Profile 模型、候选文件仓储、Notion 默认预填和登录后的 Web 设置页；
3. 实现固定 root Profile apply unit、systemd/实际 Nginx 强校验、正式 Profile 原子替换和失败回退；
4. 建立项目、实例、白名单和规则模型、校验器及确定性 Nginx 生成器，并完成单元测试；
5. 实现规则草稿文件仓储和服务端渲染表单；
6. 实现当前版本读取、配置预览和差异；
7. 实现 root publish/recover oneshot、Profile 漂移复验、`nginx -T` 接入校验、定向 reload 和异常恢复；
8. 实现项目级历史列表与恢复；
9. 在隔离 Nginx 环境分别完成 Notion 默认布局和自定义路径/service Profile 的集成测试；
10. 人工接入 Sub2API 实例 include，按首次接入验收逐项验证。

每个阶段只实现本阶段需要的能力；第一版验收完成前不扩展多主机、规则组、数据库或公共 API。

## 17. 上线前待核实项

- 目标主机经 Web 候选和 root apply 解析出的 Nginx Profile：二进制、主配置、可选 prefix、systemd service 和实际 `http` include 布局；这些值是部署结果，不再作为 Debian 专属代码常量；
- Cloudflare real IP 配置是否已被当前 `http` 配置加载，以及 `$remote_addr` 的实际值；
- Sub2API 主实例实际站点文件、业务 `location` 和反代端口；
- 管理页面最终采用 SSH 隧道还是已有的受控内网访问方式；服务默认仍只绑定 `127.0.0.1:7582`；
- 应用用户、用户组、安装路径及 sudoers 精确命令，需在部署方案确定后写入现有发布清单；
- 当前手工 ACL 切换到生成配置时的维护窗口和回退步骤。

当前 Notion 实践没有记录 Nginx 二进制和主配置的绝对路径，因此上线记录必须采用 root apply 强校验后的实际 Profile，不得用本文 JSON 示例替代。以上待核实项影响部署，不阻塞模型、生成器和本地页面实现；其中 Profile、全局 include 和 real IP 校验不通过会阻塞 Profile 应用、规则发布或恢复，但不阻止管理员登录 Web 修正候选配置。任何生产配置修改、systemd/sudoers 安装和现有 Nginx include 改动均需在实施前单独确认。

## 18. 参考资料

- [Notion：GCP Debian 13｜在 2 GB 主机裸机部署 Sub2API 与 Nginx HTTPS（2026-08-14 已验证）](https://app.notion.com/p/GCP-Debian-13-2-GB-Sub2API-Nginx-HTTPS-3bbee25b1bc18139989ccc2e349a72fe)
- [Nginx `geo` 模块官方文档](https://nginx.org/en/docs/http/ngx_http_geo_module.html)
- [Nginx `map` 模块官方文档](https://nginx.org/en/docs/http/ngx_http_map_module.html)
- [Nginx rewrite/return 官方文档](https://nginx.org/en/docs/http/ngx_http_rewrite_module.html)
- [Nginx `include` 官方文档](https://nginx.org/en/docs/ngx_core_module.html#include)
- [Nginx real IP 模块官方文档](https://nginx.org/en/docs/http/ngx_http_realip_module.html)
- [Nginx 命令行参数官方文档](https://nginx.org/en/docs/switches.html)
- [Go Argon2 包文档](https://pkg.go.dev/golang.org/x/crypto/argon2)
- [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
